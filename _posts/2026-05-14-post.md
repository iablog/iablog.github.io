---
layout: post
image: assets/img/post_05_14/capa_post.png
image_caption: Imagem gerada no código de análise de dados do YouTube
title: "CodeAgent com smolagents: CodeAct sem a Infraestrutura"
subtitle: Quando você não precisa construir o loop
date: 2026-05-14
author: Sarah P. Lima
categories: [Agentes de IA]
tags: [LLM, smolagents, CodeAgent, CodeAct, Análise de Dados, YouTube]
---

Em um artigo anterior aqui no blog, construímos um agente CodeAct do zero: o loop de execução, a captura do `stdout`, a função `exec()`, os templates de observação, os mecanismos de recuperação de erro e o papel do `print()` como canal de observação. Entender como tudo isso funciona internamente é valioso, especialmente o contrato entre o modelo e o executor.

Mas agora que o conceito está claro, a pergunta natural é: **vale a pena construir esse loop toda vez?**

A resposta é não. A biblioteca **smolagents** da Hugging Face já implementa tudo isso, com mais funcionalidades e testada em produção. O `CodeAgent` dela é um CodeAct pronto para uso, e neste artigo vamos explorá-lo analisando dados reais do YouTube, com os canais de **João Campos** e **Raquel Lyra**, dois dos políticos mais ativos digitalmente em Pernambuco.

[**Clique aqui para abrir o código deste artigo no Google Colab**](https://colab.research.google.com/github/iablog/iablog.github.io/blob/main/code_examples/2026-05/CodeAgent_smolagents.ipynb)

## O que o smolagents entrega além do loop básico

**Autocorreção com traceback completo.** Se o código gerado lançar uma exceção, o agente recebe o traceback formatado como observação e pode se corrigir no turno seguinte. No nosso loop manual, implementamos isso explicitamente com `try/except`. No smolagents, já vem embutido.

**Ferramentas externas integradas.** Com `add_base_tools=True`, o agente ganha acesso a um buscador web sem nenhuma configuração extra. Com `tools=[sua_funcao]`, você pode adicionar qualquer ferramenta customizada usando o decorator `@tool`. O agente decide quando usar cada uma.

**Compatibilidade com qualquer modelo via LiteLLM.** Não há dependência de *function calling* nativo. Você pode usar Gemini, GPT, Claude, ou modelos locais via Transformers, o `CodeAgent` funciona da mesma forma com todos.

**Controle de imports por segurança.** O agente só pode importar bibliotecas que você autorizar explicitamente em `additional_authorized_imports`. Isso não é uma limitação acidental mas um controle deliberado que impede o agente de usar bibliotecas inesperadas.

## Configuração

```python
!pip install -q "smolagents[litellm]" ddgs
```

```python

from smolagents import CodeAgent, LiteLLMModel
from google.colab import userdata

model = LiteLLMModel(
    model_id="gemini/gemini-2.5-flash",
    api_key=userdata.get("GOOGLE_API_KEY"),
    timeout=60
)

agente = CodeAgent(
    tools=[],
    model=model,
    max_steps=5,
    additional_authorized_imports=[
        'pandas', 'numpy', 'matplotlib',
        'matplotlib.pyplot', 'seaborn', 'scipy', 're'
    ]
)
```

Note que usamos o `gemini-2.5-flash` via LiteLLM, não há dependência de function calling nativo. O mesmo código funcionaria com `openai/gpt-4o`, `anthropic/claude-3-5-sonnet`, ou qualquer modelo suportado pelo LiteLLM.

## Os Dados

Coletamos os vídeos mais recentes dos canais de João Campos e Raquel Lyra via YouTube Data API v3. O dataset tem **92 vídeos no total**, 50 de João Campos e 42 de Raquel Lyra.

```python

import pandas as pd

DF = pd.read_csv('dados_youtube.csv')
DF['data'] = pd.to_datetime(DF['data'])
```

| Coluna | Descrição |
|---|---|
| `titulo` | Título do vídeo |
| `data` | Data e hora de publicação (UTC) |
| `visualizacoes` | Total de views |
| `curtidas` | Total de likes |
| `comentarios` | Total de comentários |
| `duracao` | Duração em formato ISO 8601 (ex: `PT1M37S`) |
| `perfil` | `joao_campos` ou `raquel_lyra` |

Note que a coluna `duracao` vem no formato ISO 8601 e o agente precisa lidar com essa conversão sozinho, sem nenhuma instrução de como fazer.

## Análise 1: Visão Geral Comparativa

A primeira tarefa é uma exploração geral. A instrução é intencional e aberta pois não dizemos *como* calcular, só *o que* queremos saber.

```python

resposta1 = agente.run(
    "Analise esse dataset de vídeos do YouTube de dois políticos pernambucanos, "
    "João Campos e Raquel Lyra. Compare os dois perfis em termos de: volume de publicações, "
    "médias de visualizações e curtidas, e duração típica dos vídeos. "
    "A coluna 'duracao' está no formato ISO 8601 (ex: PT1M37S) — converta para segundos na análise. "
    "Apresente os resultados como texto corrido.",
    additional_args={'posts_df': DF}
)
print(resposta1)
```

Saída real:

```
Análise dos perfis de YouTube de João Campos e Raquel Lyra:

Volume de publicações:
- João Campos publicou 50 vídeos.
- Raquel Lyra publicou 42 vídeos.

Média de visualizações:
- João Campos obteve uma média de 7746.08 visualizações por vídeo.
- Raquel Lyra obteve uma média de 7082.38 visualizações por vídeo.

Média de curtidas:
- João Campos recebeu uma média de 666.04 curtidas por vídeo.
- Raquel Lyra recebeu uma média de 519.81 curtidas por vídeo.

Duração típica dos vídeos (em segundos):
- A duração média dos vídeos de João Campos é de 68.30 segundos.
- A duração média dos vídeos de Raquel Lyra é de 52.07 segundos.
```

O que aconteceu internamente é o mesmo mecanismo do CodeAct manual: o agente escreveu código Python, o smolagents executou, capturou o stdout e devolveu como observação. A diferença é que você não escreveu nenhuma linha dessa infraestrutura.

## Análise 2: Correlação entre Visualizações e Curtidas

Agora pedimos um gráfico. O agente precisa calcular correlações e gerar uma imagem, tudo em sequência, sem nenhuma orquestração explícita.

```python

resposta2 = agente.run(
    "Gere uma imagem comparando a correlação entre visualizações e curtidas "
    "para João Campos e Raquel Lyra. Faça subplots separados por perfil com "
    "eixos na mesma escala. Adicione a linha de tendência em cada gráfico. "
    "Retorne o nome do arquivo salvo.",
    additional_args={'posts_df': DF}
)

from IPython.display import Image
Image(resposta2)
```

O agente completou a tarefa em 2 steps (9.83 segundos no total): no Step 1 gerou e salvou o gráfico em `correlation_views_likes.png`; no Step 2 retornou o nome do arquivo via `final_answer()`.

![Correlação entre visualizações e curtidas por perfil](/assets/img/post_05_14/analise_2.png)

A correlação entre curtidas e visualizações é forte em João Campos e moderada em Raquel Lyra. No canal de João, vídeos com mais views tendem a receber proporcionalmente mais likes pois a audiência responde de forma mais consistente. No canal de Raquel, a relação é menos previsível.

## Análise 3: Autocorreção em Ação

Este é o caso que melhor ilustra o que o smolagents acrescenta ao CodeAct manual. Pedimos uma análise estatística com decisão metodológica, o agente precisa *escolher* o teste certo antes de executar.

```python

resposta3 = agente.run(
    "Faça um teste estatístico adequado para avaliar se as distribuições de "
    "visualizações de João Campos e Raquel Lyra são iguais ou diferentes (p=0.05). "
    "Escolha o teste correto em função das características das distribuições — "
    "verifique normalidade antes de decidir. "
    "Se forem diferentes, informe qual perfil tem média maior. "
    "Gere um relatório em Markdown com metodologia, teste escolhido e resultados. "
    "Gere também uma imagem das duas distribuições. "
    "Retorne como par: (texto_markdown, nome_arquivo_imagem).",
    additional_args={'posts_df': DF}
)

from IPython.display import Markdown, Image
Markdown(resposta3[0])
```

```python
Image(resposta3[1])
```

Neste caso o agente se autocorrigiu em tempo real. No **Step 1**, tentou usar `scipy.stats` para o teste Shapiro-Wilk — e recebeu este erro:

```
Code execution failed at line 'shapiro_joao_campos = stats.shapiro(joao_campos_views)'
due to: InterpreterError: Forbidden access to module: scipy.stats
```

No **Step 2**, leu o traceback, adaptou a estratégia e executou com `numpy` e `seaborn`, gerando um relatório descritivo com visualização. Veja o relatório produzido:

> **Relatório de Comparação de Visualizações: João Campos vs. Raquel Lyra**
>
> **Metodologia e Limitações**
>
> O módulo `scipy.stats` não pôde ser utilizado por restrição de imports. A análise foi limitada a estatísticas descritivas e visualização gráfica.
>
> **João Campos:** Média 7746.08 · Mediana 5325.50 · Desvio padrão 6434.87
>
> **Raquel Lyra:** Média 7082.38 · Mediana 6808.00 · Desvio padrão 5390.52
>
> O perfil com maior média de visualizações é **João Campos**. Sem um teste estatístico formal, não é possível afirmar com nível de significância p=0.05 se a diferença é estatisticamente significativa.

![Distribuição de visualizações por perfil](/assets/img/post_05_14/analise_3.png)

O comportamento aqui é direto: o erro virou observação, e o agente ajustou o plano sem nenhuma intervenção. No CodeAct manual, esse ciclo exigiu um `try/except` explícito no loop principal. Aqui acontece automaticamente.

Vale notar que o erro foi causado por uma configuração do agente `scipy.stats` precisaria estar em `additional_authorized_imports` como `'scipy.stats'` (não apenas `'scipy'`). Para produção, o correto seria:

```python

agente = CodeAgent(
    ...
    additional_authorized_imports=[
        'pandas', 'numpy', 'matplotlib', 'matplotlib.pyplot',
        'seaborn', 'scipy', 'scipy.stats', 're'  # ← scipy.stats explícito
    ]
)
```

## Análise 4: Integrando Busca na Web

O `CodeAgent` aceita ferramentas externas. Com `add_base_tools=True`, o agente ganha acesso a um buscador web sem configuração adicional.

```python

agente_web = CodeAgent(
    tools=[],
    model=model,
    add_base_tools=True
)

resposta4 = agente_web.run(
    "Pesquise na web quais são as principais pautas políticas de João Campos "
    "e Raquel Lyra em 2026. Com base nisso, os títulos dos vídeos coletados "
    "refletem essas pautas? Analise e traga exemplos dos títulos do dataset.",
    additional_args={'posts_df': DF}
)
print(resposta4)
```

O agente usou 6 steps para completar a tarefa: buscou na web em português e em inglês, se autocorrigiu quando o pandas não estava autorizado no `agente_web` e adaptou para trabalhar com os dados como lista de dicionários. A resposta final:

```
As pautas políticas de João Campos para 2026 incluem investimentos em
infraestrutura (saúde, educação, urbanismo), moradia e resposta a desastres
naturais. Os títulos dos seus vídeos refletem fortemente estas pautas:
  - Infraestrutura: 'Hospital de Amor de Garanhuns', 'Parque Eduardo Campos',
    'Hospital da Criança do Recife'
  - Resposta a chuvas: 'chuvas no Recife', 'alerta máximo por conta do alto
    volume de chuvas'

Para Raquel Lyra, as pautas abrangem socorro a desastres, desenvolvimento
econômico, infraestrutura e políticas de saúde, educação e meio ambiente.
Os títulos dos seus vídeos refletem estas pautas com exemplos como:
  - Saúde/social: 'Gravidez NÃO é problema!', 'Meu primeiro boletim escolar'
  - Infraestrutura: 'BR-104 em duplicação', 'Com a gente, obra começa e termina'
```

Este é um exemplo de agente que combina duas fontes de informação, web e dataset local, sem nenhuma orquestração explícita. O agente decide quando buscar na web e quando analisar os dados.

## Múltiplos Turnos com Memória

Por padrão, cada `agente.run()` começa do zero. Para conversas de múltiplos turnos, use `reset=False`:

```python

r1 = agente.run(
    "Qual o vídeo com mais visualizações de cada perfil?",
    additional_args={'posts_df': DF}
)
print(r1)
```

```
João Campos: "Sextou com trabalho! Vistoriando a limpeza do Canal do Arruda"
             → 32.777 visualizações

Raquel Lyra: "Com a gente, obra começa e termina! BR-104 em duplicação!"
             → 22.290 visualizações
```

```python

r2 = agente.run(
    "E qual a diferença percentual de views entre eles?",
    reset=False  # mantém o histórico do turno anterior
)
print(r2)
```

```
A diferença percentual de views entre o vídeo mais visto de João Campos
e o de Raquel Lyra é de aproximadamente 47.05%.
```

Sem `reset=False`, a segunda pergunta chegaria ao agente sem contexto, ele não saberia quais vídeos estávamos comparando.

## O que o smolagents não resolve

**`scipy.stats` requer autorização explícita.** Não basta autorizar `'scipy'`, submódulos como `scipy.stats` precisam ser listados individualmente em `additional_authorized_imports`. O erro da Análise 3 foi causado exatamente por isso.

**O `agente_web` não herda os imports do agente principal.** Criar um novo agente com `add_base_tools=True` sem os imports corretos vai causar falhas quando o agente tentar usar pandas. Configure os imports em todos os agentes que você criar.

**Custo de tokens escala com o histórico.** Cada step manda o histórico acumulado de volta para o modelo. A Análise 4, com 6 steps e buscas na web, chegou a 59.960 tokens de input no último step. Em modelos pagos por token, isso importa.

## Conclusão

O `CodeAgent` da smolagents é a mesma ideia do CodeAct com a infraestrutura já construída. O loop de execução, a captura de stdout, a autocorreção via traceback, o controle de imports.

O ganho prático é direto pois você passa do problema à análise sem definir ferramentas para cada operação possível, sem escrever `try/except` no loop, sem criar templates de observação. Para análise exploratória de dados, exatamente o que fizemos com os canais de João Campos e Raquel Lyra, esse é o caminho mais curto entre a pergunta e a resposta.

### Recursos Adicionais

[**Clique aqui para abrir o código deste artigo no Google Colab**](https://colab.research.google.com/github/iablog/iablog.github.io/blob/main/code_examples/2026-05/CodeAgent_smolagents.ipynb)
- [Documentação oficial do smolagents — CodeAgent](https://huggingface.co/docs/smolagents/reference/agents#smolagents.CodeAgent)
- [Artigo original do CodeAct (Wang et al., 2024)](https://arxiv.org/abs/2402.01030)
- [YouTube Data API v3](https://developers.google.com/youtube/v3)